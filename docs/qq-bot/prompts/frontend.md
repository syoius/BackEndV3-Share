# 前端执行 prompt

在实际网页前端仓库使用。把本目录的总进度、共享契约与前端方案作为输入提供；不要假定当前工作区就是前端仓库。

已确认前端路径为 `/home/syoius/YuanHub`；仍需以执行时工作区和 AGENTS.md 为准。

```text
请在当前前端仓库实现“连接到 QQ”，放在用户现有“连接到 MaaYuan”入口旁，并复用现有账号中心、登录流程和设计风格。

先阅读当前仓库 AGENTS.md，然后阅读已提供的后端设计文档：
- docs/qq-bot/00-overview-and-progress.md
- docs/qq-bot/01-contract.md
- docs/qq-bot/03-frontend-plan.md
- docs/qq-bot/05-report-and-card-images.md
- docs/qq-bot/06-bot-snapshot-page.md
这些路径属于 BackEndV3-Share；若它们不在当前仓库，从已提供资料或可读的后端检出读取。确实缺少时索取文档位置，不能凭空编造契约。

先定位实际的 MaaYuan 连接入口、JWT 请求/刷新、子账号列表、组件与路由。不要根据旧文档猜组件路径，不重做整套账号中心。

要实现：
1. 连接到 QQ 卡片，未连接/已连接状态与管理入口。
2. 网页起点：选机器人、多个子账号及权限 → 展示 /绑定 code → 轮询 QQ 验证 → 核对真实 QQ 和授权内容 → 网页最终确认。
3. QQ 起点授权落地页：读取短期 request/ticket → 完成已有网页登录 → POST claim → 清理 ticket → 选择多个子账号/权限 → 明确 confirm。
4. 连接管理：展示所有本人子账号和 Bot 授权；完整替换 scopes；当前选中账号；其他未授权账号名称开关；单账号凭证换发；整条连接解绑。
5. 根据权限目录只展示已实现能力；读取为推荐项、写入默认关闭且依赖同域读取。B3 开放写权限后不能自动给既有连接增权。
6. 支持过期、取消、重复加载、已绑定冲突、revision 冲突、网络失败、429、清空全部授权及删除子账号后的状态刷新。
7. F3 图片渲染：复用 src/components/inventory/AcquiredPeriodReport.vue、src/data/inventory/acquiredStats.js；从 src/pages/operator/index.vue 的 agent-ledger-card 抽出共享只读呈现组件。新增 src/pages/bot/snapshot.vue 和 /bot/snapshot/:snapshotId 路由，跟随网站部署。页面只取 URL fragment 的短期 snapshot_ticket，清理片段，用专用 GET 读取固定数据；不接收 JWT/长期 Bot Token/服务秘密。meta.botSnapshot 限定跳过 authInit、hash 锚点滚动及 App 全局 SSE/弹窗，普通页面行为保持正常。#bot-report-root 提供 loading/ready/error 状态，图片保留原统计口径、已保存状态、日期/账号和数据完整性提示，移除编辑控件。不可另外手写近似模板或为插件另造渲染包。
8. 能力目录开放后展示库存报告和养成卡两项独立读取权限，清楚说明历史/关注/备注范围，既有授权不自动增权。F3 在 B4 契约就绪即可推进，不必等写权限 F2。

必须遵守：
- 一个 QQ 连接主账号，下面多个子账号分别授权；新子账号不自动勾选或授权。
- 网页账号选择用稳定 ID，不能用名称做主键；QQ 身份不可编辑。
- 已授权子账号及权限在 QQ 默认可见；“其他未授权账号名称”默认关闭，不等于数据授权。
- 只创建请求、打开链接、完成登录或 claim 都不能自动授权。最终按钮前显示完整 QQ、机器人和允许访问的子账号/权限，提醒只授权自己的 QQ。
- 连接管理页只调用 JWT 接口；snapshot 页仅用短票据调用专用 GET。任何网页都不领取/存储/展示长期 Bot Token，也不配置机器人服务秘密。
- QQ 绑定 ticket 只为本标签页短期登录恢复保存，消费/取消/过期后清理；snapshot_ticket 只放截图页内存，不进入任何持久存储。两者都不写埋点、日志或错误上报。
- 轮询只在等待状态进行，到期/离开/取消/终态即停止。服务器 expires_at 为准。
- API success/error 解析按共享契约局部适配；复用现有请求模块，不全局破坏旧接口。
- 保存用 expected_revision，冲突后让用户复核；不自动覆盖新权限。操作成功前不要先显示解绑/保存成功。
- 遵循当前仓库设计规范，新增 UI 时使用适用设计技能和既有组件；重点验证手机宽度、键盘/焦点、错误提示与复制反馈。

阶段：先完成 F1 双入口与只读授权管理，再在后端 B3 可用时核验 F2 写权限。若后端未启动，仍可实现页面和契约适配，但 mock 通过仅标记“已实现待联调”。不要自建模拟接口后宣称后端已完成。

交付代码、网站 snapshot 页面、必要的状态/权限测试、同数据网页/图片对比、仓库要求的构建检查、实际页面和接口联调结果。把 F1/F2/F3 的真实进度、改动位置、测试结果及待办回填总进度；不能写后端仓库时输出可供协调者回填的记录。未经授权不部署线上、不调用真实用户账号进行修改。
```
